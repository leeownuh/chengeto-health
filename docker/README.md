# Docker (Placeholder)

Container orchestration for this project is defined at the repo root:

- `docker-compose.yml`
- `docker-compose.prod.yml`

This folder is reserved for future deployment artifacts (e.g., Kubernetes manifests, reverse-proxy configs, additional Dockerfiles) if the project is extended beyond Compose.


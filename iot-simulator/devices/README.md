# IoT Simulator Devices (Placeholder)

The runnable IoT device simulator code lives in:

- `iot-simulator/src/devices/`

This folder is kept as a placeholder for future documentation assets and/or additional device definitions that may be separated from `src/` during hardware integration.


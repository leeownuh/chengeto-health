# Protocols (Placeholder)

Protocol implementations used by the simulator currently live under:

- `iot-simulator/src/`

This folder is reserved for future protocol adapters (e.g., MQTT topic contracts, command/ack flows) when integrating real hardware.

